# Homebrew Tap for QuotaBar

Install QuotaBar:

```bash
brew install --cask chilohwei/quotabar/quotabar
```

Or tap first:

```bash
brew tap chilohwei/quotabar
brew install --cask quotabar
```

QuotaBar releases are published from:

https://github.com/chilohwei/QuotaBar/releases
